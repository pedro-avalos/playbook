---

- name: Install group packages
  ansible.builtin.dnf:
    name:
      - "@core"
      - "@development tools"
      - "@virtualization"
    state: latest
  become: true
  become_user: root
  tags:
    - packages
